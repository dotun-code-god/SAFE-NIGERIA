{
  "name": "safe-nigeria-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": {
    "packages": [
      "apps/frontend"
    ],
    "nohoist": [
      "**/react-map-gl",
      "**/react-map-gl/**",
      "**/mapbox-gl",
      "**/mapbox-gl/**"
    ]
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
